buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://maven.pkg.github.com/facebook/react-native' }
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.0.2") // Ensure this is the correct version for your project
        classpath("com.facebook.react:react-native-gradle-plugin:0.75.2") // Correct version
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.7.10")
    }
}
