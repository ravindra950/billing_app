pluginManagement {
    repositories {
        maven {
            url("$rootDir/../node_modules/react-native/android")
        }
        google()
        mavenCentral()
        maven { url 'https://maven.pkg.github.com/facebook/react-native' } // Add this for safety
    }
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

rootProject.name = "billing"
include(":app")
